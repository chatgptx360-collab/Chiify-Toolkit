/**
 * Dashboard components.
 *
 * Presentation only — they receive already-formatted values as props. When
 * Phase 2 introduces real project data, it adds the data layer above these
 * components rather than reaching into them.
 */
export { StatCard, type StatCardProps } from './stat-card'
